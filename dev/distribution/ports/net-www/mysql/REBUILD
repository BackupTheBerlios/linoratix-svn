#
#rebuild file for mysql
#
%author: ogu	
%package-group: net-www -
%sub-group: mysql -
%required: ("libglibc >=2.3.4", "libopenssl >=0.9.7e", "zlib >=1.2.1", "gcc >=3.4.3")
%build-required: ()
%description: A fast SQL database server
%name: mysql
%version: 4.1.10
%build: $DATA
%sourcefile: mysql-4.1.10.tar.gz
%source-url: ("ftp://ftp.orst.edu/pub/mysql/Downloads/MySQL-4.1")
%conflicts: ()

# build ...
__DATA__
lip_extract %sourcefile
# -- execution of pre-install-scipts
lip_cd %compile_dir
# lip_patch %patch_dir/<patchfile>
#lip_patch -Np1 -i %patch_dir/linuxthreads-2.2.2.patch
lip_configure --prefix=/usr --disable-dependency-tracking --without-bench --without-debug --enable-assembler --localstatedir=/var/lib/mysql --mandir=/usr/share/man --with-extra-charset=all --with-pthread --with-named-thread-libs=-lpthread
lip_make
lip_install DESTDIR=%install_dir
# andere moeglichkeiten, DESTDIR=%install_dir ROOT_DIR=%install_dir, DEST_DIR=%install_dir
#   prefix=%install_dir/usr infodir=%info_dir mandir=%man_dir
#   oder andere
# if your not sure READ the MAKEFILE
# -- execution of post-install-scripts
__DATA__

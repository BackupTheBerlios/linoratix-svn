#!/bin/sh

source /etc/rc.d/functions

write_message "Loading kernel modules..."
for mod in $(cat /etc/conf.d/modules.conf)
do
	echo -n "  "
	write_message $mod
	/sbin/modprobe $mod
	check_success $?
done


#
# package build script for findutils
#
%author: jfried
%package-group: sys-utils -
%sub-group: findutils -
%required: ("sys-libs/glibc/libglibc >=2.3.4-r2")
%build-required: ()
%description: The findutils package consists of three programs. `find' is a program which searches a directory tree to find a file or group of files. It walks the directory tree and reports all occurences of a file matching the user's specifications. `locate' scans one or more databases of filenames and displays any matches. `xargs' builds and executes command lines by gathering together arguments it reads on the standard input. Most often, these arguments are lists of file names generated by `find'.
%name: findutils
%version: 4.1.20
%build: $DATA
%sourcefile: findutils-4.1.20.tar.gz
%source-url: ("http://download.linoratix.com/distfiles")

__DATA__
lip_extract %sourcefile
# -- execution of pre-install-scripts
lip_cd %compile_dir
lip_sed -i '/^SUBDIRS/s/locate//' Makefile.in

lip_configure --prefix=/usr --libexecdir=/usr/lib/locate --mandir=/usr/share/man --infodir=/usr/share/info

lip_make
# lip_install DESTDIR=%install_dir
lip_install prefix=%install_dir/usr libexecdir=%install_dir/usr/lib/locate mandir=%install_dir/usr/share/man infodir=%install_dir/usr/share/info libdir=%install_dir/usr/lib
# -- execution of post-install-scritps
__DATA__

